# Tasks

- [ ] Replace to_string_pretty() with to_string() in all rayo-mcp tool handlers
- [ ] Replace to_string_pretty() with to_string() in rayo-profiler export
- [ ] Remove redundant page.title() and page.url() calls from navigate goto handler
- [ ] Verify integration tests pass with compact JSON
