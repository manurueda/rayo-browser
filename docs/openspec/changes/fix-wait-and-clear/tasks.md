# Tasks

- [ ] Add null-safe fallback in MutationObserver JS (document.body || document.documentElement)
- [ ] Replace execCommand clear with value assignment + event dispatch
- [ ] Verify build, lint, and tests pass
