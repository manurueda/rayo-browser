# Tasks

- [ ] Replace .lock().unwrap() in start_span() (2 sites) with graceful handling
- [ ] Replace .lock().unwrap() in spans() with graceful handling returning empty vec
- [ ] Replace .lock().unwrap() in reset() with graceful handling
- [ ] Replace .lock().unwrap() in span_count() with graceful handling returning 0
- [ ] Verify build, lint, and tests pass
