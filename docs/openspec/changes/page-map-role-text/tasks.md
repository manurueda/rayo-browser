# Tasks

- [x] Update the full page-map extractor to populate `item.text` for `role="button"`, `role="link"`, and `role="tab"`
- [x] Update the scoped page-map extractor in `browser.rs` with the same behavior
- [x] Add regression coverage that verifies both full and scoped page maps include text for role-based interactive elements
- [x] Validate with targeted `rayo-core` tests and OpenSpec validation if available
